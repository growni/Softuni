function solve(matrix) {
    //TODO: first create the bigger matrix and then fill the inner matrix.

    let outerMatrix = [];
    console.log(matrix.length);
    outerMatrix.length = matrix.length + 2;
    for (let row = 0; row < matrix.length + 2; row++) {
       
    }
    console.log();
}

solve([['2', '3', '4', '7', '0'],
['4', '0', '5', '3', '4'],
['2', '3', '5', '4', '2'],
['9', '8', '7', '5', '4']]);